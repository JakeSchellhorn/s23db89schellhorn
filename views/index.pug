extends layout

block content
  h1= title
  p This is a default filed created with #{title}
  button(type="button", class="btn btn-secondary", onclick="goFaster()") Press me
  div(id="faster") Increase the speed

  p Link to 
    a(href="/table") search results

  script.  
    mph = 20;
    change = 1;
    function goFaster() {
        mph = mph + change;
        change = change + 1;
        document.getElementById("faster").innerHTML= `MPH ${mph} with change ${change}`
     }